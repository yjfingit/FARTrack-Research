task: >-
  Cross-model, training-free RGB single-object tracking research on frozen
  SiamRPN++, OSTrack, MixFormer-Online, ODTrack, and FARTrackSparse. Test
  whether internal risk prediction transfers into positive conditional action
  advantage under a B_dev-only calibration/selection protocol.
coordinator:
  max_cycles: 5
  ui:
    interaction_mode: auto
