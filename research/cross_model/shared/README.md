# Shared Assets

Only read-only evaluation adapters, metric schemas, and cross-model table
generators belong here. Model-specific source modifications belong in that
model's isolated data-disk worktree, not in this coordinator checkout.
