# Cross-Model Paper Layout

The manuscript will live in this directory. Bibliography, figures, tables, and
LaTeX build artifacts are intentionally separated so that the source tree is
portable and does not reference data-disk figure paths.
